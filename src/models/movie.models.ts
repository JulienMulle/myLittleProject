export class MOVIE {
    id: number;
    title: string;
    episode: number;

    constructor(id: number, title: string, episode: number) {
        this.id = id;
        this.title = title;
        this.episode = episode;
    }
}